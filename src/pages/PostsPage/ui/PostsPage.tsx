import { Page } from "@/shared/ui/Page";
import { PostList } from "@/entities/Post";

const PostsPage = () => {
  return (
    <Page>
      <PostList />
    </Page>
  );
};

export default PostsPage;
